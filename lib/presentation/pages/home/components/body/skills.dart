import 'package:flutter/material.dart';

class SkillsBodyComponent extends StatelessWidget {
  const SkillsBodyComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
